# Keep the crate-level unwrap_used/expect_used/panic = "deny" gate for src/, but allow the
# ergonomic unwrap/expect inside #[cfg(test)] modules and integration tests (parity with the
# memory-server crate's clippy.toml).
allow-unwrap-in-tests = true
allow-expect-in-tests = true

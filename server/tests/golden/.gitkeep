# Golden-vector fixtures for embedding determinism tests (§5 best-practice #3):
# a frozen (model.onnx, tokenizer.json, pooling, normalization) → committed vector.
# Assert cosine >= 0.9999 (or abs tol 1e-4) in the release profile you gate on.
